{
  "name": "ngResolve",
  "version": "0.0.1",
  "description": "A simple angular module to load static json data and make it available with a service",
  "main": "dist/ngResolve",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.6.2",
    "gulp-clean": "^0.2.4",
    "gulp-concat": "^2.2.0",
    "gulp-connect": "^2.0.5",
    "gulp-load-plugins": "^0.5.1",
    "gulp-ngmin": "^0.3.0",
    "gulp-uglify": "^0.3.0"
  }
}
